{
  "name": "cbl-aufgeweckt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node pruefe.mjs",
    "serve": "npx http-server -p 8080 -c-1",
    "pdf": "npx decktape reveal --size 1920x1080 --pause 3000 --load-pause 3000 \"http://localhost:8080/?nofrag\" aufgeweckt-ki-klartext.pdf",
    "pruefe": "node pruefe-folien.mjs http://localhost:8080 ./.pruefung",
    "pruefe-karte": "node pruefe-karte.mjs ./.pruefung"
  },
  "devDependencies": {
    "decktape": "^3.15.0",
    "puppeteer": "^24.0.0"
  }
}
